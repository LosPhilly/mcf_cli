name: mcf_feature
description: Generates a full Mission-Critical Flutter lib.
version: 1.0.0

environment:
  mason: ">=0.1.0 <0.2.0"

vars:
  name:
    type: string
    description: The name of the feature (e.g., 'user', 'order')
    default: feature
    prompt: What is the name of the feature?